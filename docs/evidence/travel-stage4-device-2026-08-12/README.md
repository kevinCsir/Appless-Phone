# 阶段四真机验收记录

- 设备：`6HR0226506005010`
- 安装：阶段四 Debug HAP 覆盖安装成功。
- 实际查询：深圳到厦门，12306 返回 30 条高铁方案；航班供应商请求成功但没有可展示航班，界面保留真实失败原因。
- 过期保护：超过 30 分钟的结果禁止继续选择或预订，并提示重新查询。
- 重新查询：从已选且过期的交通状态可重新发起查询，旧选择被清除，新结果和查询时间正常写入。
- 筛选：飞机筛选显示 `0 / 30` 和明确空状态；高铁筛选显示 `6 / 30`，杀进程重启后仍保持激活。
- 方案选择：G3020 展示 Agent 推荐、最早到达、门到门 5 小时 30 分、建议离家 05:21、最终到达 10:51；逐条选择成功。
- 换选：点击“换一个方案”后清除选中态，候选、筛选和任务上下文保留。
- 恢复：重新选择 G3020 后杀进程重启，仍显示“已选择”、高铁筛选、`6 / 30` 和“换一个方案”。
- 自动化：Hypium `Tests run: 1942, Failure: 0, Error: 0, Pass: 1942, Ignore: 0`。

证据截图：`selected-restart.jpeg`。
