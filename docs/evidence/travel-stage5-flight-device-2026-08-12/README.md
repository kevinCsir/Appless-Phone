# 阶段五航班真机验收证据

- 设备：`6HR0226506005010`
- 日期：2026-08-12
- 构建：阶段五 Debug HAP，覆盖安装成功
- 验收数据：Debug 专用非实时样例 `TEST8888`，深圳宝安 → 上海虹桥，日期 `2026-08-21`
- 测试边界：没有使用真实航班、价格或订单数据，没有登录、下单或支付

## 验收结果

1. `flight-booking-fixture.jpeg`：已选航班进入“确认交通预订”，绑定“去携程预订航班”和“换一个方案”。样例仅用于稳定复现真机流程。
2. `ctrip-official-domain-whaleguard-block.jpeg`：点击后由系统浏览器打开携程官方域名 `flights.ctrip.com`；页面正文被设备浏览器的 WhaleGuard 拦截，因此本次只确认官方域名承接成功，不能视为携程页面内容加载通过。
3. `flight-return-verification.jpeg`：返回 App 后进入“核验交通订单”，明确提示“打开页面不等于完成预订”，并提供核验、重新打开、用户确认和未完成动作。
4. `flight-sync-unsupported.jpeg`：点击“核验供应商订单”后如实显示“供应商暂未提供订单查询接口”，没有伪造订单号、支付或出票结果。
5. `flight-process-restore.jpeg`：杀进程并重新启动 App 后仍恢复到“核验交通订单”，返回点与待核验状态持久化成功。
6. `original-train-task-restored.jpeg`：航班样例归档后，原有厦门高铁任务及进度恢复，验收过程没有覆盖用户已有任务。

## 结论

App 侧航班“选择方案 → 打开官方供应商 → 返回核验 → 无接口时给出真实边界 → 进程恢复”已通过真机验证。携程页面正文加载仍受 WhaleGuard 阻断，真实航班搜索、登录、下单、支付和出票不在本次通过范围内。
