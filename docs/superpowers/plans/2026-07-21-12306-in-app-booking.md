# 12306 In-App Booking Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有 12306 高铁卡片的时刻行右侧显示真实的 `¥xxx起`，在展开态提供“去购票”，并在 App 内以手机可读方式打开 12306 官网登录、选车次、下单与支付流程。

**Architecture:** 继续由 `train.search` 读取 12306 同一份余票响应，在 `ToolGatewayClient` 内解析 `yp_info_new` 的最低可售价格并生成线路级官网 URL；通过现有 A2UI → HTML 快照 → 出行 Renderer 将价格放入时刻行并加入客户端动作；`Index` 只接受当前文档中的动作且再次校验 URL，然后打开扩展后的 `CheckoutWebOverlay`。共用 WebView 只增加一个移动可读开关，12306 关闭整页缩小并放大文字；不新增工具、支付 provider、页面路由、本地订单状态或网页注入。

**Tech Stack:** HarmonyOS ArkTS、现有 A2UI JSONL 数据模型、HtmlTravelHomeRenderer、ArkWeb `A2uiWebSurfaceView`、Hypium、DevEco `hvigorw`、HarmonyOS 真机 `hdc`。

## Global Constraints

- 实施前读取并遵循 `superpowers:test-driven-development`；每个行为先出现可解释的 RED，再写最小 GREEN。
- 保持 Ponytail 边界：复用现有 `CheckoutWebOverlay`，不新增购票页、provider adapter、后端接口、依赖或通用导航框架。
- 价格只能来自本次 12306 响应的 `yp_info_new`；不得估算、补默认价或额外发起 N+1 票价请求。
- 折叠态不显示席别，只在价格真实存在时显示 `¥xxx起`；展开态按钮文案固定为“去购票”。
- 12306 没有稳定的指定车次深链，因此官网 URL 只预填日期、出发站和目的地，用户在官网重新选择车次。
- 不注入或读取 12306 账号、乘车人、订单和支付信息；关闭 WebView 后不生成本地成功状态。
- 自定义 Scheme 默认阻止。只有真机实际观察且确认来自官方流程的 Scheme，才能另行加入精确白名单；本计划不猜测 Scheme。
- Hypium 结果必须读取 `entry/.test/default/intermediates/test/coverage_data/test_result.txt`，不能只看命令退出码。
- 规格文档：`docs/superpowers/specs/2026-07-21-12306-in-app-booking-design.md`。

---

### Task 1: 从现有 12306 响应提取真实最低价并生成官网 URL

**Files:**
- Modify: `agent_core/src/main/ets/aiphone/runtime/A2uiTypes.ets`
- Modify: `agent_core/src/main/ets/aiphone/runtime/ToolGatewayClient.ets`
- Modify: `entry/src/test/ToolGatewayClient.test.ets`

**Interfaces:**
- `A2uiTrainData.price?: string`
- `A2uiTrainData.bookingUrl?: string`
- `trainLowestAvailablePriceForTest(row: string): string`
- `build12306BookingUrl(date: string, fromName: string, fromCode: string, toName: string, toCode: string): string`

- [ ] **Step 1: 记录当前基线**

Run:

```bash
cd /Users/luoyige/DevEcoStudioProjects/AIPhoneDemo
git status --short
DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk \
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw \
  --mode module -p module=entry@default -p product=default test --no-daemon
test -f entry/.test/default/intermediates/test/coverage_data/test_result.txt
rg -n "Total:|Passed:|Failure:|Error:" \
  entry/.test/default/intermediates/test/coverage_data/test_result.txt
```

Expected: 记录基线总数；`Failure: 0` 且 `Error: 0`。若基线已有失败，先保存完整结果并停止，不把它归因于本功能。

- [ ] **Step 2: 写价格和 URL 的失败测试**

在 `ToolGatewayClient.test.ets` 的 `@loop/agent-core` import 中加入：

```ts
build12306BookingUrl,
trainLowestAvailablePriceForTest,
```

在测试文件的 fixture 区加入：

```ts
function trainRowForPrice(
  ypInfoNew: string,
  business: string,
  first: string,
  second: string,
  noSeat: string = '',
  hardSeat: string = '',
  softSleeper: string = ''
): string {
  const fields: string[] = [];
  for (let index = 0; index < 40; index++) {
    fields.push('');
  }
  fields[23] = softSleeper;
  fields[26] = noSeat;
  fields[29] = hardSeat;
  fields[30] = second;
  fields[31] = first;
  fields[32] = business;
  fields[39] = ypInfoNew;
  return fields.join('|');
}
```

在现有 `toolGatewayClient` 测试组中加入以下三个 case：

```ts
it('uses the lowest priced seat that is currently available', 0, () => {
  const row = trainRowForPrice(
    '9174800000M093300000O055300000',
    '有',
    '2',
    '有'
  );
  expect(trainLowestAvailablePriceForTest(row)).assertEqual('553');
});

it('ignores cheaper unavailable, waitlist, and malformed train prices', 0, () => {
  expect(trainLowestAvailablePriceForTest(
    trainRowForPrice('O040000000M055300000', '', '3', '无')
  )).assertEqual('553');
  expect(trainLowestAvailablePriceForTest(
    trainRowForPrice('O040000000', '', '', '候补')
  )).assertEqual('');
  expect(trainLowestAvailablePriceForTest(
    trainRowForPrice('O0553', '', '', '有')
  )).assertEqual('');
  expect(trainLowestAvailablePriceForTest(
    trainRowForPrice('OABCDE0000', '', '', '有')
  )).assertEqual('');
  expect(trainLowestAvailablePriceForTest(
    trainRowForPrice('', '', '', '有')
  )).assertEqual('');
});

it('builds the official route-level 12306 booking URL', 0, () => {
  const url = build12306BookingUrl(
    '2026-07-22',
    '深圳北',
    'IOQ',
    '香港西九龙',
    'XJA'
  );
  expect(url).assertEqual(
    'https://kyfw.12306.cn/otn/leftTicket/init?linktypeid=dc' +
    '&fs=%E6%B7%B1%E5%9C%B3%E5%8C%97%2CIOQ' +
    '&ts=%E9%A6%99%E6%B8%AF%E8%A5%BF%E4%B9%9D%E9%BE%99%2CXJA' +
    '&date=2026-07-22&flag=N,N,Y'
  );
});
```

- [ ] **Step 3: 运行 RED**

Run the Task 1 Step 1 Hypium command again, then inspect `test_result.txt`.

Expected: compile/test fails only because the two new exports do not exist. If failure is unrelated, fix the test seam before writing production code.

- [ ] **Step 4: 扩展高铁数据模型**

在 `A2uiTrainData` 末尾加入可选字段：

```ts
price?: string;
bookingUrl?: string;
```

Do not make them required; old A2UI snapshots must continue compiling.

- [ ] **Step 5: 实现最小价格解析**

在 `parse12306Row` 前加入以下 helpers。字段映射使用当前 12306 余票响应：商务 32、特等 25、一等 31、二等 30、高级软卧 21、软卧/一等卧 23、动卧 33、硬卧/二等卧 28、软座 24、硬座 29、无座 26。

```ts
function trainField(fields: string[], index: number): string {
  return fields.length > index ? fields[index].trim() : '';
}

function isAvailableTrainSeat(value: string): boolean {
  const text = value.trim();
  return text === '有' || /^[1-9]\d*$/.test(text);
}

function trainSeatAvailabilityForPriceCode(code: string, fields: string[]): string {
  if (code === '9') {
    return trainField(fields, 32);
  }
  if (code === 'P') {
    return trainField(fields, 25);
  }
  if (code === 'M') {
    return trainField(fields, 31);
  }
  if (code === 'O' || code === 'S') {
    return trainField(fields, 30);
  }
  if (code === '6' || code === 'A') {
    return trainField(fields, 21);
  }
  if (code === '4' || code === 'I') {
    return trainField(fields, 23);
  }
  if (code === 'F') {
    return trainField(fields, 33);
  }
  if (code === '3' || code === 'J') {
    return trainField(fields, 28);
  }
  if (code === '2') {
    return trainField(fields, 24);
  }
  if (code === '1') {
    return trainField(fields, 29);
  }
  if (code === 'W') {
    return trainField(fields, 26);
  }
  return '';
}

function lowestAvailable12306Price(fields: string[]): string {
  const ypInfoNew = trainField(fields, 39);
  if (ypInfoNew.length === 0 || ypInfoNew.length % 10 !== 0) {
    return '';
  }
  let minimumJiao = -1;
  for (let offset = 0; offset < ypInfoNew.length; offset += 10) {
    const chunk = ypInfoNew.substring(offset, offset + 10);
    const availability = trainSeatAvailabilityForPriceCode(chunk.substring(0, 1), fields);
    const priceText = chunk.substring(1, 6);
    if (!isAvailableTrainSeat(availability) || !/^\d{5}$/.test(priceText)) {
      continue;
    }
    const priceJiao = Number.parseInt(priceText, 10);
    if (priceJiao <= 0) {
      continue;
    }
    if (minimumJiao < 0 || priceJiao < minimumJiao) {
      minimumJiao = priceJiao;
    }
  }
  if (minimumJiao < 0) {
    return '';
  }
  const yuan = Math.floor(minimumJiao / 10);
  const jiao = minimumJiao % 10;
  return jiao === 0 ? yuan.toString() : yuan.toString() + '.' + jiao.toString();
}

export function trainLowestAvailablePriceForTest(row: string): string {
  return lowestAvailable12306Price(row.split('|'));
}
```

在 `parse12306Row` 返回对象中加入：

```ts
price: lowestAvailable12306Price(fields),
```

- [ ] **Step 6: 生成并透传线路级官网 URL**

在 `ToolGatewayClient.ets` 加入：

```ts
export function build12306BookingUrl(
  date: string,
  fromName: string,
  fromCode: string,
  toName: string,
  toCode: string
): string {
  return 'https://kyfw.12306.cn/otn/leftTicket/init?linktypeid=dc' +
    '&fs=' + encodeURIComponent(fromName + ',' + fromCode) +
    '&ts=' + encodeURIComponent(toName + ',' + toCode) +
    '&date=' + encodeURIComponent(date) +
    '&flag=N,N,Y';
}
```

将 normalizer 改为显式保留新字段：

```ts
function normalizeTrainRecord(item: A2uiTrainData, bookingUrl: string = ''): A2uiTrainData {
  return {
    trainCode: item.trainCode,
    from: item.from,
    to: item.to,
    fromLocation: item.fromLocation,
    depart: item.depart,
    arrive: item.arrive,
    duration: item.duration,
    seats: item.seats.length > 0 ? item.seats : formatTrainSeatsSummary(item),
    status: item.status.length > 0 ? item.status : '计划',
    business: item.business,
    first: item.first,
    second: item.second,
    noSeat: item.noSeat,
    hardSeat: item.hardSeat,
    sleeperSoft: item.sleeperSoft,
    price: item.price === undefined ? '' : item.price,
    bookingUrl: bookingUrl
  };
}
```

在 `callLocalTrainSearch` 解析完 `date/fromName/fromCode/toName/toCode` 后只构建一次：

```ts
const bookingUrl = build12306BookingUrl(date, fromName, fromCode, toName, toCode);
```

并将 top 构造改为：

```ts
top.push(normalizeTrainRecord(parsed[index], bookingUrl));
```

同时把 `trainToTravelOption` 的固定空价格改为：

```ts
price: item.price === undefined ? '' : item.price,
```

这只让现有综合出行卡复用同一真实价格，不给综合出行新增购票动作或数据模型。

- [ ] **Step 7: 运行 GREEN 并提交**

Run the Hypium command and inspect `test_result.txt` again.

Expected: 新增测试通过；全量 `Failure: 0`、`Error: 0`。

```bash
git diff --check
git add \
  agent_core/src/main/ets/aiphone/runtime/A2uiTypes.ets \
  agent_core/src/main/ets/aiphone/runtime/ToolGatewayClient.ets \
  entry/src/test/ToolGatewayClient.test.ets
git commit -m "feat: expose real 12306 train prices"
```

---

### Task 2: 在现有高铁卡片加入简洁价格和“去购票”动作

**Files:**
- Modify: `entry/src/main/ets/pages/A2uiHome/html/HtmlHomeSnapshot.ets`
- Modify: `entry/src/main/ets/pages/A2uiHome/html/HtmlTravelHomeRenderer.ets`
- Modify: `entry/src/test/HtmlHomeSnapshot.test.ets`
- Modify: `entry/src/test/HtmlHomeRenderer.test.ets`
- Modify: `entry/src/test/HtmlHomeActionPolicy.test.ets`

**Interfaces:**
- New current-document action: `train.booking.open`
- Action args: `{ bookingUrl: string }`
- Existing `isHtmlHomeActionAllowed` remains unchanged and performs exact snapshot matching.

- [ ] **Step 1: 写 HTML 快照 RED**

在 `HtmlHomeSnapshot.test.ets` 的 train fixture 中加入：

```json
"price":"553",
"bookingUrl":"https://kyfw.12306.cn/otn/leftTicket/init?linktypeid=dc&fs=test&ts=test&date=2026-07-22&flag=N,N,Y"
```

将 train block assertions 改为：

```ts
expect(JSON.stringify(doc.blocks[0].metrics).indexOf('"label":"price","value":"553"')).assertLarger(-1);
expect(doc.blocks[0].actions.length).assertEqual(2);
expect(doc.blocks[0].actions[0].id).assertEqual('train.booking.open');
expect(doc.blocks[0].actions[0].label).assertEqual('去购票');
expect(doc.blocks[0].actions[0].variant).assertEqual('primary');
expect(JSON.stringify(doc.blocks[0].actions[0].args).indexOf('"bookingUrl":"https://kyfw.12306.cn/')).assertLarger(-1);
expect(doc.blocks[0].actions[1].id).assertEqual('html_pin_train');
```

再加入一个明确的旧数据兼容 case：

```ts
it('keeps legacy train rows without price or booking action', 0, () => {
  const trains: A2uiComponent = {
    id: 'legacy-trains',
    component: 'TrainOptions',
    dataPath: '/trains'
  };
  const doc = createHtmlHomeDocument({
    surface: surface('travel', 'ready', [root('legacy-trains'), trains],
      '{"trains":[{"trainCode":"G1","from":"深圳北","to":"上海虹桥","depart":"09:00","arrive":"15:30","duration":"6h30m","seats":"二等座有票","status":"available","business":"","first":"2","second":"有","noSeat":"","hardSeat":"","sleeperSoft":""}]}'),
    messages: [],
    isBusy: false,
    isAwaitingFirstSurface: false
  });

  expect(JSON.stringify(doc.blocks[0].metrics).indexOf('"label":"price"')).assertEqual(-1);
  expect(doc.blocks[0].actions.length).assertEqual(1);
  expect(doc.blocks[0].actions[0].id).assertEqual('html_pin_train');
});
```

- [ ] **Step 2: 写 Renderer 与动作策略 RED**

在 `travelFoodCardsDocumentFixture()` 的 train block 中加入：

```ts
{ label: 'price', value: '553' }
```

并将 train actions 设为：

```ts
actions: [{
  id: 'train.booking.open',
  label: '去购票',
  prompt: '',
  variant: 'primary',
  kind: 'client',
  args: {
    bookingUrl: 'https://kyfw.12306.cn/otn/leftTicket/init?linktypeid=dc&fs=test&ts=test&date=2026-07-22&flag=N,N,Y'
  }
}, {
  id: 'html_pin_travel',
  label: '收藏行程',
  prompt: 'pin train',
  variant: 'secondary',
  kind: 'client'
}]
```

在对应 Renderer test 中加入：

```ts
expect(html.indexOf("var price = metric(block, ['price']);")).assertLarger(-1);
expect(html.indexOf("'¥' + price + '起'")).assertLarger(-1);
expect(html.indexOf('去购票')).assertLarger(-1);
expect(html.indexOf('二等座起')).assertEqual(-1);
expect(html.indexOf('train.booking.open')).assertLess(html.indexOf('html_pin_travel'));
```

在 `HtmlHomeActionPolicy.test.ets` 增加当前文档精确匹配测试：

```ts
it('allows only the visible 12306 booking action arguments', 0, () => {
  const doc = documentFixture();
  const bookingUrl =
    'https://kyfw.12306.cn/otn/leftTicket/init?linktypeid=dc&fs=test&ts=test&date=2026-07-22&flag=N,N,Y';
  const visible: HtmlHomeAction = {
    id: 'train.booking.open',
    label: '去购票',
    prompt: '',
    variant: 'primary',
    kind: 'client',
    args: { bookingUrl: bookingUrl }
  };
  doc.blocks[0].actions = [visible];
  const allowed: A2uiAction = {
    id: 'train.booking.open',
    label: '去购票',
    prompt: '',
    variant: 'primary',
    kind: 'client',
    args: { bookingUrl: bookingUrl }
  };
  const forged: A2uiAction = {
    id: 'train.booking.open',
    label: '去购票',
    prompt: '',
    variant: 'primary',
    kind: 'client',
    args: { bookingUrl: 'https://evil.example/' }
  };

  expect(isHtmlHomeActionAllowed(doc, allowed)).assertTrue();
  expect(isHtmlHomeActionAllowed(doc, forged)).assertFalse();
});
```

不要给 action policy 增加 train 特例。

- [ ] **Step 3: 运行 RED**

Run the Hypium command from Task 1.

Expected: snapshot 缺少 price/action，Renderer 缺少价格构造，因此新增断言失败；旧测试保持原状。

- [ ] **Step 4: 在快照中透传价格并把购票动作放首位**

在 `appendTrainBlocks` 的 duration 后加入：

```ts
appendMetric(block.metrics, 'price', item.price);
```

在 pin action 前加入：

```ts
if (item.bookingUrl !== undefined && item.bookingUrl.trim().length > 0) {
  const bookingArgs: Object = {
    bookingUrl: item.bookingUrl.trim()
  };
  block.actions.push({
    id: 'train.booking.open',
    label: '去购票',
    prompt: '',
    variant: 'primary',
    kind: 'client',
    sourceComponentId: component.id,
    args: bookingArgs
  });
}
```

保留现有 pin action 紧随其后。不要修改 `TrainOptionsView`。

- [ ] **Step 5: 用现有 metric 样式渲染折叠价**

将 `HtmlTravelHomeRenderer.ets` 中固定的 duration array 替换为：

```js
var summaryMetrics = [
  ['耗时', metric(block, ['duration']) || '待确认']
];
var price = metric(block, ['price']);
if (source === 'train' && price.length > 0) {
  summaryMetrics.push(['', '¥' + price + '起']);
}
summaryMetrics.forEach(function (item) {
  var box = add(metrics, 'div', 'journey-metric');
  if (item[0].length > 0) {
    add(box, 'span', '', item[0]);
  }
  add(box, 'b', '', item[1]);
});
```

Do not add a seat label, badge, icon, new color, new spacing token, or new CSS component. `addActionButtons` already makes the first action primary and stops card click propagation.

- [ ] **Step 6: 运行 GREEN、检查最小 diff 并提交**

Run Hypium and inspect `test_result.txt`.

Expected: 新旧快照、Renderer、action policy tests 全部通过；旧 train data 无价格、无购票按钮。

```bash
git diff --check
git diff --stat
git add \
  entry/src/main/ets/pages/A2uiHome/html/HtmlHomeSnapshot.ets \
  entry/src/main/ets/pages/A2uiHome/html/HtmlTravelHomeRenderer.ets \
  entry/src/test/HtmlHomeSnapshot.test.ets \
  entry/src/test/HtmlHomeRenderer.test.ets \
  entry/src/test/HtmlHomeActionPolicy.test.ets
git commit -m "feat: add 12306 booking action to train cards"
```

Expected diff boundary: 两个 production HTML files、三个既有 tests；没有新组件或样式文件。

---

### Task 3: 将现有结算 WebView 扩展为安全的 12306 官网购票层

**Files:**
- Modify: `entry/src/main/ets/pages/A2uiHome/components/CheckoutWebOverlay.ets`
- Create: `entry/src/test/CheckoutWebOverlay.test.ets`
- Modify: `entry/src/test/List.test.ets`

**Interfaces:**
- `CheckoutWebKind = 'payment' | 'hotel' | 'train'`
- `isAllowed12306BookingUrl(url: string): boolean`
- Existing `checkoutWebNavigationDecision(kind: CheckoutWebKind, url: string, isMainFrame: boolean)`

- [ ] **Step 1: 写 URL 与导航策略 RED**

创建 `CheckoutWebOverlay.test.ets`：

```ts
import { describe, it, expect } from '@ohos/hypium';
import {
  checkoutWebNavigationDecision,
  isAllowed12306BookingUrl
} from '../main/ets/pages/A2uiHome/components/CheckoutWebOverlay';

export default function checkoutWebOverlayTest() {
  describe('checkoutWebOverlay', () => {
    it('accepts only the official 12306 route entry URL', 0, () => {
      expect(isAllowed12306BookingUrl(
        'https://kyfw.12306.cn/otn/leftTicket/init?date=2026-07-22'
      )).assertTrue();
      expect(isAllowed12306BookingUrl(
        'http://kyfw.12306.cn/otn/leftTicket/init?date=2026-07-22'
      )).assertFalse();
      expect(isAllowed12306BookingUrl(
        'https://kyfw.12306.cn.evil.example/otn/leftTicket/init'
      )).assertFalse();
      expect(isAllowed12306BookingUrl(
        'https://kyfw.12306.cn/otn/leftTicket/query'
      )).assertFalse();
      expect(isAllowed12306BookingUrl(
        'https://kyfw.12306.cn/otn/leftTicket/init.evil'
      )).assertFalse();
    });

    it('keeps HTTPS train navigation in WebView and blocks unverified schemes', 0, () => {
      expect(checkoutWebNavigationDecision(
        'train',
        'https://kyfw.12306.cn/otn/leftTicket/init',
        true
      )).assertEqual('allow');
      expect(checkoutWebNavigationDecision(
        'train',
        'https://epay.12306.cn/pay/webBusiness',
        true
      )).assertEqual('allow');
      expect(checkoutWebNavigationDecision('train', 'alipays://platformapi/startapp', true)).assertEqual('block');
      expect(checkoutWebNavigationDecision('train', 'javascript:alert(1)', true)).assertEqual('block');
      expect(checkoutWebNavigationDecision('train', 'alipays://platformapi/startapp', false)).assertEqual('allow');
    });

    it('does not regress payment and hotel decisions', 0, () => {
      expect(checkoutWebNavigationDecision(
        'payment',
        'aiphone://payment/success',
        true
      )).assertEqual('payment_return');
      expect(checkoutWebNavigationDecision(
        'hotel',
        'https://evil.example/',
        true
      )).assertEqual('block');
    });
  });
}
```

在 `List.test.ets` import 并调用：

```ts
import checkoutWebOverlayTest from './CheckoutWebOverlay.test';
```

```ts
checkoutWebOverlayTest();
```

- [ ] **Step 2: 运行 RED**

Run Hypium and inspect `test_result.txt`.

Expected: `train` union member 和 validator 尚不存在，产生明确 compile failure。

- [ ] **Step 3: 实现严格初始 URL 校验与 HTTPS 导航边界**

在 `CheckoutWebOverlay.ets` 加入：

```ts
export type CheckoutWebKind = 'payment' | 'hotel' | 'train';

export function isAllowed12306BookingUrl(url: string): boolean {
  const value = url.trim();
  const authority = 'https://kyfw.12306.cn';
  const lower = value.toLowerCase();
  if (!lower.startsWith(authority)) {
    return false;
  }
  const suffix = value.substring(authority.length);
  const path = '/otn/leftTicket/init';
  if (!suffix.startsWith(path)) {
    return false;
  }
  const tail = suffix.substring(path.length);
  return tail.length === 0 || tail.startsWith('?') || tail.startsWith('#');
}
```

在现有 payment/hotel decisions 后加入：

```ts
if (kind === 'train' && !value.startsWith('https://')) {
  return 'block';
}
```

Initial action URL 由 `Index` 使用 `isAllowed12306BookingUrl` 严格校验；进入官网以后，只允许主页面 HTTPS 导航留在 WebView。不要把任意自定义 Scheme 加入 allow。

- [ ] **Step 4: 复用现有 overlay UI**

加入：

```ts
@State currentUrl: string = '';
```

将标题与按钮文案扩展为：

```ts
private title(): string {
  if (this.kind === 'train') {
    return '12306 官方购票';
  }
  return this.kind === 'hotel' ? 'RollingGo 酒店预订' : '安全支付确认';
}

private closeLabel(): string {
  if (this.kind === 'train') {
    return '返回车次';
  }
  return this.kind === 'hotel' ? '返回房型' : '关闭';
}
```

增加一个只用于显示的 host helper，并让 train 的 `domainText()` 从 `currentUrl`（为空时用 `url`）提取实际 host：

```ts
function webHostForDisplay(url: string): string {
  const value = url.trim();
  const schemeEnd = value.indexOf('://');
  if (schemeEnd < 0) {
    return '';
  }
  const start = schemeEnd + 3;
  let end = value.indexOf('/', start);
  if (end < 0) {
    end = value.length;
  }
  const query = value.indexOf('?', start);
  if (query >= 0 && query < end) {
    end = query;
  }
  const fragment = value.indexOf('#', start);
  if (fragment >= 0 && fragment < end) {
    end = fragment;
  }
  return value.substring(start, end);
}
```

在 `domainText()` 的 hotel 分支后、payment 逻辑前加入：

```ts
if (this.kind === 'train') {
  const activeUrl = this.currentUrl.length > 0 ? this.currentUrl : this.url;
  const host = webHostForDisplay(activeUrl);
  return host.length > 0 ? host : 'kyfw.12306.cn';
}
```

`A2uiButtonView` 的 `label` 改为 `this.closeLabel()`；`onLoadEnd` 中先更新真实 URL 再记安全日志：

```ts
this.currentUrl = url;
aiLogInfo('[AIPhone][CheckoutWebLoadEnd] kind=' + this.kind + ' chars=' + url.length.toString());
```

train 被拦截时显示：

```ts
this.blockedNavigation = '已阻止离开安全的 HTTPS 页面。';
aiLogInfo('[AIPhone][TrainBookingNavigationBlocked] chars=' + url.length.toString());
```

不要记录完整 URL，不修改现有 JavaScript、DOM Storage、popup 或 error-page 配置。

- [ ] **Step 5: 运行 GREEN 并提交**

Run Hypium and inspect `test_result.txt`.

Expected: train、payment、hotel navigation tests 全部通过；`Failure: 0`、`Error: 0`。

```bash
git diff --check
git add \
  entry/src/main/ets/pages/A2uiHome/components/CheckoutWebOverlay.ets \
  entry/src/test/CheckoutWebOverlay.test.ets \
  entry/src/test/List.test.ets
git commit -m "feat: support official 12306 in-app web booking"
```

---

### Task 4: 将卡片动作安全接到 WebView，关闭后只提示官方结果边界

**Files:**
- Modify: `entry/src/main/ets/pages/A2uiHome/Index.ets`
- Test: `entry/src/test/HtmlHomeActionPolicy.test.ets`
- Test: `entry/src/test/CheckoutWebOverlay.test.ets`

**Interfaces:**
- `TrainBookingActionPayload { bookingUrl?: HtmlActionValue }`
- Existing `showCheckoutWeb`, `checkoutWebKind`, `checkoutWebUrl`

- [ ] **Step 1: 确认前两层安全测试已覆盖 wiring 前置条件**

Run Hypium and verify these named cases are present and passing in `test_result.txt`:

- current-document action exact match;
- forged action args rejected;
- official initial URL accepted;
- HTTP、伪造 host、错误 path rejected;
- unverified main-frame Scheme blocked.

Expected: all pass before touching `Index.ets`.

- [ ] **Step 2: 增加 action payload 和 URL 提取**

将 import 改为：

```ts
import {
  CheckoutWebKind,
  isAllowed12306BookingUrl
} from './components/CheckoutWebOverlay';
```

在 `PaymentCheckoutActionPayload` 邻近加入：

```ts
interface TrainBookingActionPayload {
  bookingUrl?: HtmlActionValue;
}
```

在现有 action URL helpers 邻近加入：

```ts
private trainBookingUrlForAction(action: A2uiAction): string {
  if (action.args === undefined) {
    return '';
  }
  const payload = action.args as TrainBookingActionPayload;
  return this.actionText(payload.bookingUrl).trim();
}
```

- [ ] **Step 3: 在现有 client action handler 打开 train overlay**

紧邻 `payment.checkout.open` 分支加入：

```ts
if (action.id === 'train.booking.open') {
  const bookingUrl = this.trainBookingUrlForAction(action);
  if (!isAllowed12306BookingUrl(bookingUrl)) {
    this.logHtmlHomeActionError(action, 'invalid_12306_booking_url');
    return;
  }
  this.checkoutWebKind = 'train';
  this.checkoutWebUrl = bookingUrl;
  this.showCheckoutWeb = true;
  aiLogInfo('[AIPhone][TrainBookingOpen] urlChars=' + bookingUrl.length.toString());
  return;
}
```

Do not call `payment.send`, `openExternalUrl`, any provider backend, or any order-status method here.

- [ ] **Step 4: 关闭购票页后恢复卡片并显示真实边界**

将 HomePage props 中的 `onCloseCheckoutWeb` 改为：

```ts
onCloseCheckoutWeb: () => {
  const closedTrainBooking = this.checkoutWebKind === 'train';
  this.showCheckoutWeb = false;
  if (closedTrainBooking) {
    this.applyPanelState(updateConnectionPanelState(this.currentPanelState(), {
      ok: true,
      status: 'ready',
      message: '订单与支付结果请以 12306 为准。',
      requestDebug: this.lastModelRequest,
      assistantDebug: this.lastModelOutput,
      error: ''
    }));
  }
},
```

Do not modify `currentSurface` and do not create `orderId`, paid/success flags, or local receipt data.

- [ ] **Step 5: 运行完整 GREEN、静态检查并提交**

```bash
DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk \
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw \
  --mode module -p module=entry@default -p product=default test --no-daemon
rg -n "Total:|Passed:|Failure:|Error:" \
  entry/.test/default/intermediates/test/coverage_data/test_result.txt
rg -n "train\.booking\.open|TrainBookingOpen|订单与支付结果请以 12306 为准" \
  entry/src/main/ets/pages/A2uiHome/Index.ets
git diff --check
```

Expected: `Failure: 0`、`Error: 0`；只存在一个 train action branch；没有本地成功状态。

```bash
git add entry/src/main/ets/pages/A2uiHome/Index.ets
git commit -m "feat: open 12306 booking from train cards"
```

---

### Task 5: 更新能力说明并完成构建、真机和安全边界验收

**Files:**
- Modify: `docs/current-capabilities.md`
- Verify only: all production and test files changed above
- Artifacts: existing ignored `tool-gateway/.smoke/` and build output only

- [ ] **Step 1: 更新真实能力边界**

将 `train.search` 行的表现更新为：

```text
12306 真实余票与最低可售价格；展开后可在 App 内打开 12306 官网继续购票，指定车次需在官网重选，订单与支付结果以 12306 为准
```

保持 capability 为 `read`；不要宣称新增 create/order/payment API。

- [ ] **Step 2: 做最终 Hypium 与范围检查**

```bash
cd /Users/luoyige/DevEcoStudioProjects/AIPhoneDemo
DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk \
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw \
  --mode module -p module=entry@default -p product=default test --no-daemon
test -f entry/.test/default/intermediates/test/coverage_data/test_result.txt
rg -n "Total:|Passed:|Failure:|Error:" \
  entry/.test/default/intermediates/test/coverage_data/test_result.txt
rg -n "mock|fake|estimated price|估算票价|购票成功|支付成功" \
  agent_core/src/main/ets/aiphone/runtime/ToolGatewayClient.ets \
  entry/src/main/ets/pages/A2uiHome/html/HtmlHomeSnapshot.ets \
  entry/src/main/ets/pages/A2uiHome/html/HtmlTravelHomeRenderer.ets \
  entry/src/main/ets/pages/A2uiHome/components/CheckoutWebOverlay.ets \
  entry/src/main/ets/pages/A2uiHome/Index.ets
git diff --check
```

Expected: Hypium `Failure: 0`、`Error: 0`；forbidden scan 没有本功能引入的伪价格或成功文案。若仓库旧文本命中，逐条确认并记录，不机械删除无关代码。

- [ ] **Step 3: 构建 signed HAP**

```bash
node /Applications/DevEco-Studio.app/Contents/tools/hvigor/hvigor/bin/hvigor.js \
  --mode module -p module=entry@default -p product=default assembleHap --no-daemon
test -f entry/build/default/outputs/default/entry-default-signed.hap
```

Expected: ArkTS compile 与 HAP packaging success。

- [ ] **Step 4: 安装到唯一 HarmonyOS 真机并证明不依赖 Mac gateway**

```bash
TARGET="$(hdc list targets | sed -n '1p')"
test -n "$TARGET"
test "$(hdc list targets | sed '/^[[:space:]]*$/d' | wc -l | tr -d ' ')" = "1"
hdc fport ls
hdc -t "$TARGET" install -r \
  entry/build/default/outputs/default/entry-default-signed.hap
```

Expected: 只有一个设备；安装成功；`hdc fport ls` 为空。不要启动 Mac gateway，也不要设置新的端口转发。

- [ ] **Step 5: 跑真实 train.search 并核对卡片**

```bash
AIPHONE_HDC_TARGET="$TARGET" \
AIPHONE_QUERY_TIMEOUT_MS=90000 \
AIPHONE_QUERY_RETRY_LIMIT=2 \
node scripts/aiphone-device-smoke.mjs \
  '帮我查询明天晚上六点以后深圳北到香港西九龙的高铁'
```

Expected:

- tool selection is real `train.search` and runtime is `local://aiphone-tools`;
- no Mac gateway or mock result;
- if current 12306 response has a valid available-seat price, folded card shows exactly one `¥xxx起` and no seat label beside it;
- if current response has no valid price, the price node is absent rather than filled with a fallback value;
- expand retains seat details and shows primary “去购票”, with pin secondary.

Record PASS, PARTIAL, BLOCKED, or FAIL against the live provider response; do not turn provider emptiness into a fabricated PASS.

- [ ] **Step 6: 人工点“去购票”并验证官网流程边界**

On the device:

1. Expand one real train card and tap “去购票”.
2. Verify foreground bundle remains `com.jiuwen.appless` and overlay title is “12306 官方购票”.
3. Verify the loaded official page contains the same date/from/to route; reselect the train because no train-level deep link exists.
4. Continue only through non-destructive login/query/order-preview steps that the tester is authorized to perform. Do not submit payment in automated acceptance.
5. Close the overlay and verify the original card remains, with only “订单与支付结果请以 12306 为准。” and no local booking/payment success.

Inspect logs without storing complete URLs or credentials:

```bash
hdc -t "$TARGET" hilog -x | rg \
  "TrainBookingOpen|CheckoutWebLoadEnd.*kind=train|TrainBookingNavigationBlocked|local://aiphone-tools"
hdc fport ls
```

Expected: `TrainBookingOpen` and at least one `CheckoutWebLoadEnd kind=train`; fport remains empty.

- [ ] **Step 7: 对外部 Scheme 做证据门禁**

If no custom Scheme is required, no code change is allowed.

If the official flow emits a custom Scheme:

- keep it blocked in this run;
- record only the scheme name and the official UI step that emitted it, never the full sensitive URL;
- report `BLOCKED_EXTERNAL_SCHEME` for that payment/identity step;
- create a follow-up RED/GREEN change only after confirming the exact scheme and installed target app. That follow-up may add one exact allowlist entry and reuse the existing external URL opener; it must not add a wildcard or generic Scheme passthrough.

This is an evidence gate, not permission to guess `alipays`, `weixin`, or a 12306 app Scheme.

- [ ] **Step 8: 提交能力文档并做最终工作树审计**

```bash
git add docs/current-capabilities.md
git commit -m "docs: document 12306 in-app booking boundary"
git status --short --branch
git log --oneline -6
```

Expected: only intended source/test/docs commits; no `.env.local`, raw config, build output, smoke logs, screenshots, or credentials are tracked.

---

### Task 6: 将折叠态价格移到时刻行右侧

**Files:**
- Modify: `entry/src/main/ets/pages/A2uiHome/html/HtmlTravelHomeRenderer.ets`
- Test: `entry/src/test/HtmlHomeRenderer.test.ets`

**Interfaces:**
- Consumes: existing train `price` metric
- Produces: `.journey-price` in `.journey-mainline`; bottom `.journey-metrics` retains duration only

- [ ] **Step 1: 修改现有 Renderer 测试形成 RED**

在 `renders a provider-backed itinerary with travel controls and food results` 的价格断言后加入：

```ts
expect(html.indexOf("add(mainline, 'div', 'journey-price')")).assertLarger(-1);
expect(html.indexOf("summaryMetrics.push(['', '¥' + price + '起'])")).assertEqual(-1);
expect(html.indexOf('grid-column: 4')).assertLarger(-1);
```

保留既有 `二等座起` 不存在、`去购票` 在固定动作之前等断言。

- [ ] **Step 2: 运行 RED 并读取 Hypium 结果**

```bash
DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk \
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw \
  --mode module -p module=entry@default -p product=default test --no-daemon
tail -40 entry/.test/default/intermediates/test/coverage_data/test_result.txt
```

Expected: 该 Renderer case 因 `.journey-price` 尚不存在、价格仍被 push 到底部指标而失败；不得接受其他失败。

- [ ] **Step 3: 实现最小价格布局**

在 `.journey-mainline` 样式后加入：

```css
.journey-price {
  grid-column: 4;
  justify-self: end;
  align-self: center;
  color: var(--source);
  white-space: nowrap;
  font-variant-numeric: tabular-nums;
}
.journey-price b {
  font-size: 18px;
  line-height: 1;
  font-weight: 850;
}
```

在 `@media (max-width: 380px)` 中加入：

```css
.journey-mainline { grid-template-columns: minmax(0, 1fr) minmax(0, 1fr) auto; }
.journey-price { grid-column: 3; }
```

将现有价格逻辑从底部 `summaryMetrics` 移到到达时刻节点之后、`journey-metrics` 之前：

```js
var price = metric(block, ['price']);
if (source === 'train' && price.length > 0) {
  var priceBlock = add(mainline, 'div', 'journey-price');
  add(priceBlock, 'b', '', '¥' + price + '起');
}
var metrics = add(card, 'div', 'journey-metrics');
var summaryMetrics = [
  ['耗时', metric(block, ['duration']) || '待确认']
];
```

删除原来的 `summaryMetrics.push(['', '¥' + price + '起'])`。不要增加价格 badge、席别、动画或新数据字段。

- [ ] **Step 4: 运行 GREEN、检查并提交**

运行 Step 2 的 Hypium 命令，然后验证：

```bash
rg -n "Tests run:" entry/.test/default/intermediates/test/coverage_data/test_result.txt
git diff --check
git add \
  entry/src/main/ets/pages/A2uiHome/html/HtmlTravelHomeRenderer.ets \
  entry/src/test/HtmlHomeRenderer.test.ets
git commit -m "fix: align train price with journey times"
```

Expected: `Failure: 0`、`Error: 0`；价格只在时刻行出现一次，底部只保留耗时。

---

### Task 7: 让 12306 官网在手机上默认可读并完成返回提示

**Files:**
- Modify: `entry/src/main/ets/pages/A2uiHome/components/common/A2uiWebSurfaceView.ets`
- Modify: `entry/src/main/ets/pages/A2uiHome/components/CheckoutWebOverlay.ets`
- Modify: `entry/src/main/ets/pages/A2uiHome/Index.ets`
- Test: `entry/src/test/CheckoutWebOverlay.test.ets`
- Verify: existing ignored `.superpowers/sdd/12306-task-5-report.md` and `tool-gateway/.smoke/`

**Interfaces:**
- `checkoutWebMobileReadable(kind: CheckoutWebKind): boolean`
- `A2uiWebSurfaceView.mobileReadable: boolean = false`
- Existing train close copy: `订单与支付结果请以 12306 为准。`

- [ ] **Step 1: 为 train-only 移动显示策略写 RED**

在 `CheckoutWebOverlay.test.ets` import 中加入 `checkoutWebMobileReadable`，并增加：

```ts
it('uses mobile-readable WebView presentation only for train booking', 0, () => {
  expect(checkoutWebMobileReadable('train')).assertTrue();
  expect(checkoutWebMobileReadable('payment')).assertFalse();
  expect(checkoutWebMobileReadable('hotel')).assertFalse();
});
```

- [ ] **Step 2: 运行 RED**

运行 Task 6 Step 2 的 Hypium 命令。

Expected: compile/test 只因 `checkoutWebMobileReadable` 尚未导出而失败。

- [ ] **Step 3: 增加一个共用 WebView 移动可读开关**

在 `A2uiWebSurfaceView` props 中加入：

```ts
@Prop mobileReadable: boolean = false;
```

在主 `Web` 与 popup `Web` 的现有属性链中都加入：

```ts
.overviewModeAccess(!this.mobileReadable)
.textZoomRatio(this.mobileReadable ? 130 : 100)
```

ArkWeb 的 `zoomAccess` 默认已开启，不重复配置。不要设置自定义 User-Agent，不注入 viewport、CSS 或 JavaScript。

- [ ] **Step 4: 仅为 train overlay 开启该设置**

在 `CheckoutWebOverlay.ets` 加入：

```ts
export function checkoutWebMobileReadable(kind: CheckoutWebKind): boolean {
  return kind === 'train';
}
```

并在现有 `A2uiWebSurfaceView` 构造参数中加入：

```ts
mobileReadable: checkoutWebMobileReadable(this.kind),
```

不要改变 payment、hotel 的 URL、安全决策或默认显示。

- [ ] **Step 5: 让返回边界提示在车次页真实可见**

保留现有 `applyPanelState`，并在 `closedTrainBooking` 分支末尾加入一行原生提示：

```ts
this.getUIContext().getPromptAction().showToast({ message: '订单与支付结果请以 12306 为准。' });
```

已有真机报告中“提示只写入配置页、车次页不可见”是该行为的 RED。不要给卡片增加提示字段或本地订单状态。

- [ ] **Step 6: 运行完整 GREEN 并提交**

```bash
DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk \
/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw \
  --mode module -p module=entry@default -p product=default test --no-daemon
rg -n "Tests run:" entry/.test/default/intermediates/test/coverage_data/test_result.txt
git diff --check
git add \
  entry/src/main/ets/pages/A2uiHome/components/common/A2uiWebSurfaceView.ets \
  entry/src/main/ets/pages/A2uiHome/components/CheckoutWebOverlay.ets \
  entry/src/main/ets/pages/A2uiHome/Index.ets \
  entry/src/test/CheckoutWebOverlay.test.ets
git commit -m "fix: improve mobile 12306 booking readability"
```

Expected: `Failure: 0`、`Error: 0`；无新依赖、网页注入或本地购票状态。

- [ ] **Step 7: 重建、覆盖安装并真机复验**

按 Task 5 Steps 3–7 重建 signed HAP、覆盖安装到同一唯一设备且不 clean data，确认 `hdc fport ls` 始终为空。再次运行真实 F02，并人工验证：

1. `¥xxx起` 与发车/到达时刻同行且位于卡片右端，底部没有价格指标卡；
2. “去购票”仍打开 App 内官方 `kyfw.12306.cn`；
3. 官网不再整页缩小，默认文字可读，双指缩放可用；允许横向滚动，不要求伪装为响应式 H5；
4. 日期、出发站和目的地仍正确，指定车次仍需官网重选；
5. 关闭后原卡片保留，toast 可见，无“购票成功”或“支付成功”；
6. 不登录、不提交订单、不付款，不猜测放行任何 custom Scheme。

将新截图、layout 和 PID HiLog 保存到既有 ignored smoke 目录，并把最终 PASS/PARTIAL/BLOCKED 边界追加到 Task 5 报告。

---

### Task 8: 修复官网路线预填与 train 子框架协议边界

**Files:**
- Modify: `agent_core/src/main/ets/aiphone/runtime/ToolGatewayClient.ets`
- Modify: `entry/src/main/ets/pages/A2uiHome/components/CheckoutWebOverlay.ets`
- Modify: `entry/src/test/ToolGatewayClient.test.ets`
- Modify: `entry/src/test/CheckoutWebOverlay.test.ets`

- [ ] **Step 1: 写两个精确 RED**

将官网 URL 断言改为站名和代码分别编码、保留字面量逗号分隔符；将 train 子框架 `alipays://` 断言改为 `block`。payment、hotel 的既有行为保持不变。

- [ ] **Step 2: 运行 Hypium 并确认 RED**

Expected: 只因当前 URL 仍包含 `%2C` 且 train 子框架仍放行未知 Scheme 而失败。

- [ ] **Step 3: 做最小实现**

`build12306BookingUrl` 分别编码站名和代码，并以字面量逗号拼接。`checkoutWebNavigationDecision` 在子框架早退前先对 train 的非 HTTPS 导航返回 `block`；不改变 payment、hotel，也不扩大 train 可导航协议。

- [ ] **Step 4: 运行完整 GREEN 并提交**

Expected: `test_result.txt` 中 Failure/Error 均为 0，`git diff --check` 通过。

- [ ] **Step 5: 真机重建并复验官网表单**

覆盖安装 signed HAP 且不 clean data，保持 `hdc fport ls` 为空。再次执行真实 F02，确认官网日期、出发站、目的地不再显示 `%2C`，移动可读设置、返回 toast 与卡片状态均保持正确；不登录、不下单、不付款。

---

### Task 9: 修复真实无座最低价与 popup 当前域名回传

**Files:**
- Modify: `agent_core/src/main/ets/aiphone/runtime/ToolGatewayClient.ets`
- Modify: `entry/src/main/ets/pages/A2uiHome/components/common/A2uiWebSurfaceView.ets`
- Modify: `entry/src/test/ToolGatewayClient.test.ets`

- [ ] **Step 1: 为真实无座价格段写 RED**

使用已在官方北京至上海响应复现的 `O067203065` 形态：末四位 `3065` 表示无座库存，field 26 为可售、field 30 二等座不可售时应返回 `672`，而不是商务座 `2350`。再覆盖无座不可售时忽略该段，以及非零角价格格式。

- [ ] **Step 2: 运行 Hypium 并确认精确 RED**

Expected: 只因当前实现仍按价格代码 `W` 查无座而失败。

- [ ] **Step 3: 按官方 10 字符段语义做最小修复**

若价格段末四位为数字且数值 `>=3000`，使用 field 26 的无座可售状态；否则继续使用现有席别代码映射。不要新增 provider、估价或 fallback。

- [ ] **Step 4: 让 popup 页面结束与关闭都回传当前实际 URL**

在 popup `Web` 上加入 `onPageEnd`，仅把事件中的最终 URL交给既有 `onLoadEnd`；popup 通过关闭按钮或 `onWindowExit` 退出时，从主 controller 读取当前 URL并交给同一回调，读取失败则传空字符串以回退初始安全来源。不要增加新状态、域名解析或导航策略。这样 train overlay 继续复用既有 `currentUrl` 与 `webHostForDisplay` 对称显示 popup 或主页面域名。

- [ ] **Step 5: 完整 GREEN、构建与定向真机回归**

Expected: Hypium Failure/Error 为 0，signed HAP 构建成功；重跑真实 F02，卡片价格仍来自实时响应且位置/官网/返回不回归。若当前流程没有安全的 popup 触发点，popup 域名以源码回调链和编译验证为 PASS，不登录、不进入支付。

---

## Final Acceptance Checklist

- [ ] Real available prices come from the same 12306 response and use the lowest currently available seat price.
- [ ] Missing/malformed prices do not hide the train card and do not produce fallback values.
- [ ] Folded train cards show only `¥xxx起` at the right edge of the time row, with no bottom price metric, seat type, or extra badge.
- [ ] Expanded train cards show primary “去购票” and retain the existing pin action.
- [ ] Only a current-document `train.booking.open` with an exact official URL can open the overlay.
- [ ] Initial URL is HTTPS + exact `kyfw.12306.cn` + exact `/otn/leftTicket/init`.
- [ ] Official HTTPS navigation stays in the App WebView; unknown custom Schemes are blocked.
- [ ] The official 12306 page opens without whole-page shrink, uses train-only text zoom, and remains pinch-zoomable without DOM injection.
- [ ] Closing the overlay restores the same train surface and never claims booking/payment success.
- [ ] Hypium reports `Failure: 0` and `Error: 0` in `test_result.txt`.
- [ ] Signed HAP builds and the real-device path has evidence with empty `hdc fport ls`.
- [ ] No new page, route, provider, dependency, mock data, or local railway order state was added.

## Self-Review Before Execution

- [ ] Search for unfinished markers with `rg -n 'TO''DO|TB''D|\.{3}' docs/superpowers/plans/2026-07-21-12306-in-app-booking.md`; expected output is empty.
- [ ] Verify every named file exists except the one explicitly marked Create.
- [ ] Verify optional field types are consistent from `A2uiTrainData` through snapshot/action payload.
- [ ] Verify every behavior change has a RED and GREEN step or an explicit real-device evidence gate.
- [ ] Verify no task asks automation to log in, submit an irreversible order, or pay.
- [ ] Verify the plan remains within the confirmed design and Ponytail minimum-change boundary.
